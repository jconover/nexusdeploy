apiVersion: v1
kind: Service
metadata:
  name: worker-service
  namespace: nexusdeploy
  labels:
    app: worker-service
    app.kubernetes.io/name: worker-service
    app.kubernetes.io/component: worker
spec:
  type: ClusterIP
  selector:
    app: worker-service
  ports:
    - name: metrics
      port: 9090
      targetPort: 9090
      protocol: TCP
