---
# docker/defaults/main.yml — Default variables for docker role
docker_users:
  - nexusdeploy

docker_log_max_size: "100m"
docker_log_max_file: "5"
docker_storage_driver: overlay2

docker_registry_mirrors: []
docker_insecure_registries: []
